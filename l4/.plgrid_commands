

module avail python

module load python/3.10.8-gcccore-12.2.0-bare
module load python/3.11.3-gcccore-12.3.0


du -sh
df -h

rm -rf .venv
rm -rf ~/.cache/pip

[ares][plgwmackowiak@login01 ~]$ realpath .
/net/people/plgrid/plgwmackowiak

cd $SCRATCH
[ares][plgwmackowiak@login01 plgwmackowiak]$ realpath .
/net/afscra/people/plgwmackowiak


# tworzę venv w SCRATCH #

# 1. Przejdź do swojego osobistego, pojemnego katalogu na dysku Scratch
cd $SCRATCH
# (Jeśli po wpisaniu tego wylądujesz w swoim katalogu domowym, wpisz ręcznie: cd /net/scratch/people/plgwmackowiak )

# 2. Utwórz tam folder na swój projekt i wejdź do niego
mkdir ŚCIEŻKA_taka_sama_jak_w_homie
cd tam

# 3. Załaduj moduł i zrób środowisko wirtualne DOKŁADNIE TUTAJ
module load python/3.10.8-gcccore-12.2.0-bare
python -m venv .venv
source .venv/bin/activate

# 4. Zainstaluj PyTorcha
pip install -r requirements




# wracamy do HOME #

# i teraz już tylko odpalamy
