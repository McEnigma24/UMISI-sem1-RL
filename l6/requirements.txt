# L6 — Python 3.12.x | PyTorch z obsługą CUDA (wheel-e z indeksu PyTorch, CUDA 12.4).
# Instalacja: uv pip install -r requirements.txt
# (najpierw: uv python install 3.12 && uv venv)
#
# Pierwsza linia techniczna musi być --extra-index-url — stąd torch bierze build +cu124.

# --- środowisko MuJoCo / Fetch ---
gymnasium==1.2.2
gymnasium-robotics==1.4.2
mujoco==3.8.1

# --- numpy + PyTorch (nanodt: numpy>=2.2.5, torch>=2.7) ---
numpy>=2.2.5,<2.3

# --- ekspert PPO, logi ---
stable-baselines3[extra]>=2.5.0,<3
tensorboard==2.19.0
tqdm==4.67.1

# --- demonstracje + Decision Transformer ---
minari[hdf5]>=0.5.3,<0.6
nanodt==0.1.0
pillow>=11.2.1

# --- sprawozdanie (Jupyter) ---
matplotlib>=3.8,<4
ipykernel>=6.29

# --- narzędzia instalacyjne ---
setuptools>=65,<81
